# 编码与协作规范

## 总体原则

- 优先满足用户的最新需求和明确约束，不为复用而复用。在此边界内，输出者负责对架构方案作出明确仲裁，维护系统的概念完整性；但不得以架构统一为由忽略已确认的功能、兼容性、安全性或交付范围。
- 设计方案应考虑完整，实现应保持简单；代码量应与功能复杂度匹配，并以满足需求的最少代码元素为目标。
- 简单设计按以下顺序衡量：通过所有相关测试、消除有实际价值的重复、清晰表达意图、减少不必要的代码元素。
- 在同等满足需求的前提下，优先删除冗余逻辑而不是叠加新分支；不得为追求“减法”删除仍有契约或调用方依据的行为。
- 只给出确定、可执行的提示词和指导，禁止模糊描述。
- 当某项决策受到具体提示词影响时，明确指出对应提示词以及它如何影响了决策。

## 规格、设计与计划

- 中大型任务按“规格说明 → 设计 → 任务列表”三阶段规划；范围清晰的小任务可合并阶段，不得为形式完整制造冗余文档。
- 编码前先明确需求、接口、输入输出、数据格式、状态归属、生命周期、失败路径和验收标准。
- 计划必须列出涉及的具体文件、API、核心逻辑、关键算法、测试与验证方式。
- 计划只描述当前决定实施的方案，不保留已被替代的方案、失效方向或变更过程；历史只通过 Git 追溯。
- 计划涉及布局、架构或关键流程时，必须用 ASCII 框图、Mermaid 流程图、时序图或状态图直观展示。涉及同步锁、最终一致性、跨进程协作或其他核心状态流转时，必须使用 Mermaid 状态图或时序图核对状态权威写入者、时序、竞态和失败恢复；若存在会改变核心模型的实质性选择，获得用户确认后再编码。
- 需求不明确时，先给出能被当前信息支持的核心骨架；不得自行增加会导致范围扩张的包装或能力。

## 可读性、命名与代码结构

- 代码首先写给人读，优先清晰、直接、可预测的表达，不依赖隐晦约定。
- 变量、函数、类、事件和配置名称必须准确反映业务含义与实际用途，不以偶然的技术实现细节命名。
- 命名风格遵循当前语言和仓库约定，例如 Java/C# 成员通常使用 `camelCase`，项目名通常使用 `kebab-case`；不为引入通用风格而破坏现有一致性。
- 含义和用途相同的函数、字段和类型使用一致命名；代码、文档和讨论中的同一业务概念不得无理由使用多套术语。
- 函数应保持聚焦。参数超过 3 个时必须评估是否存在职责过多或可组合的业务概念；只有参数共同构成稳定语义时才封装为对象，不得为满足数量限制制造无含义的参数包。
- 优先使用早返回处理错误和边界情况，减少无必要的嵌套；若早返回会使资源释放或事务边界变得不清晰，则优先保证生命周期正确。
- 注释解释“为什么”、契约或非显然约束，不复述代码“做了什么”。
- `TODO` 或 `FIXME` 只用于确实需要跟进的临时问题，必须附带可执行的上下文或跟踪依据；问题解决后立即删除。

## 复用、函数与抽象

- 先检索并复用仓库已有能力。通用问题优先使用成熟第三方库，避免重复实现。
- 多处相似且语义一致的逻辑应合并为通用函数，避免复制粘贴。
- 现有逻辑与新需求部分重合时，先评估能否直接改造；若改造会明显增加复杂度，则拆分职责并仅提取真正公共的部分。
- 不为形式统一强行拆分或封装。仅转发调用的包装函数应直接内联，例如 `on_enter`、`on_exit`、`on_fire` 只转调 `on_event` 时，不保留额外间接层。
- 抽象应形成“深模块”：对外接口尽可能简单稳定，内部承担完整且内聚的能力。
- 修改预计涉及超过 5 个独立文件时，先绘制影响链并审查核心抽象。若多点修改源于职责分散或缺少共用契约，先重构核心抽象再实施；若是同一跨文件契约的必然同步更新，可直接修改，但必须在计划中说明无需新抽象的依据。

## 架构与设计

- 按问题的实际边界应用 SOLID，不得为展示原则而增加类、接口或间接层。
- 优先组合而非继承。只有存在稳定多态、多实现边界、隔离外部依赖或明确测试替身需求时，才优先面向接口设计。
- 使用设计模式前必须指明它解决的具体问题。已有模式能直接处理时优先复用，不得滥用模式。
- 描述对象关系时，必须区分“生命周期所有者”（负责创建与释放）、“状态权威写入者”（拥有可变状态的修改权）和“依赖引用”（表示调用或引用）；不得用“持有”笼统代替。
- 新功能必须延续现有接口的命名、错误模型、数据方向、生命周期和调用习惯，避免风格与概念割裂。

## 修改与重构

- 拒绝只覆盖表面症状的零散补丁。每次修改必须围绕根因和完整调用链形成一致方案，同时保持与问题规模匹配的最小改动范围。
- 调整或重构函数前，检索并理解所有调用点、引用点和相关数据流，保证整条调用链同步修改且无遗漏。
- 新增 API、函数、方法、事件、配置入口或数据通道前，必须先检索现有能力、调用方式和职责边界；能够直接使用或小幅改造时，不得新建重复接口。
- 确需新增接口时，必须明确现有能力不适用的原因、新接口的职责边界以及它与现有调用链的关系。
- 方法、函数、字段或其他定义不再使用时，必须完全移除。
- 工作流发生变化时，同步新增或更新提示词，明确指导 LLM 按新流程工作。
- 修复缺陷时，先稳定复现并定位根因，再实施修改，最后用同一复现路径和相关回归测试验证结果。
- 新增可配置内容时，必须在相应配置文件中提供配置入口。

## 调试

- 排查缺陷时，不论复杂度，必须结合日志分析。
- 缺少可用日志时，沿实际逻辑链补充能证明输入、状态变化、分支选择和失败点的 debug 日志。
- 修复并验证完成后，移除本次专为排查而新增的 debug 日志；若某条日志对长期可观测契约有必要，应按正式日志级别、结构和脱敏要求单独评估，不作为 debug 日志遗留。

## 优化与复杂度

- 收益明确且风险可控的优化，可结合实际场景主动实施，包括简化实现、减少资源消耗或改善性能。
- 收益不明确，且会显著增加实现复杂度、并发风险或架构约束的优化，必须先用性能数据验证；暂时无法验证时，先与用户沟通，不得擅自引入。
- 关键算法优先采用中级程序员容易理解的简单实现。确需复杂算法时，先说明必要性并获得用户批准。

## 测试与质量保障

- 测试覆盖应与风险和影响范围匹配，可按需组合单元测试、集成测试和端到端测试。
- 功能测试按功能集中管理，不得分散到被测配置或其他无关模块中。
- 涉及配置的功能，其测试仍归入该功能的测试位置，不在配置模块中单独编写。
- 功能较复杂时，在 `./tests` 下为该功能建立独立目录。
- 使用仓库已配置的格式化、静态分析、类型检查和测试工具；新增 ESLint、Checkstyle、SonarQube 等工具前，必须评估现有工具链、引入成本和团队维护责任，不得只为形式完整而添加。
- 代码审查优先发现缺陷、回归、安全风险、契约偏差和缺失测试，其次才是非功能性风格问题。

## 版本控制与协作

- 使用 Git 管理代码和文档变更，使每次提交围绕单一可解释的目标，不夹带无关格式或元数据变更。
- 修改已有工作树时，用户或其他协作者的改动不得擅自回滚或覆盖；重叠部分必须先理解再合并。
- 团队共同维护开发规范；当规范与已确认的项目契约冲突时，必须显式处理冲突，不得同时保留两套模糊规则。

## 时间处理

- 时间在存储、传递和内部运算时统一使用时间戳。
- 仅在需要展示或格式化时，按运行时所在时区将时间戳转换为格式化时间。

## 文档与注释维护（强制）

### 阅读与判断

- 文档和注释只是理解代码逻辑、职责边界与关键流程的线索。判断具体行为时，必须核对实际代码，不得仅依据注释、文档、记忆或当前依赖组合作出结论，也不得默认现有实现正确。

### 准入标准

文档只记录值得记的，而不是全面。需求、设计、API、部署和测试文档均按实际契约、复杂度与维护需要建立，不以类型齐全为目标。

文档和注释只记录以下内容：

- 调用契约。
- 不可破坏的设计要求。
- 非显然的生命周期、线程或失败约束。
- 必须跨文件才能理解的修改规则。
- 帮助读者定位实现、选择配置或正确修改系统的必要信息。
- 文档引用实现位置时，不要求记录精确的 `path:line`；应定位到具体函数或字段，并提供可用于检索的名称或关键词。
- 对外 API 需要交互式或机器可读文档时，优先使用仓库已有的 OpenAPI/Swagger 等生成机制，避免手工维护重复契约。

不得记录以下内容：

- 对名称或代码的简单复述。
- 当前依赖组合或配置快照。
- 暂未实现的能力。
- 仅根据“当前代码中不存在”推导出的“不持有”“不负责”或“不依赖”等结论。
- 不影响契约、兼容性或安全性的普通当前值。

### 约束

- 负面约束只有在违反它会构成缺陷，且能由仓库级规则、领域 Skill、测试或公开接口证明时才可保留。不得把对当前实现的观察提升为禁止性规则。

### 配置说明

- 需要帮助读者选择配置时，即使当前只有一个实际使用值，也应列举常用候选值，并说明各值的语义、影响、适用场景和约束。
- 除非具体值本身构成重要设计约束、兼容边界或安全要求，否则不得为同步现状而抄录 `foo = 1` 一类当前赋值。
- 必须记录具体值时，同时说明该值为何重要。

### 更新与删除

- 实现变化后，同步更新仍有效的契约，并彻底删除已过期、被取代、废弃或仅描述已删除代码的文档与注释；不得将失效内容改写成“历史参考”或“已废弃”说明。
- 对“无法运行”“不可用”等结论，在写入最终记录或持久文档前，必须先与用户确认。

## 排版原则（最小化 Git 差异）

- 只修改与任务相关的逻辑和文本，不改动无关的缩进、引号或换行符类型。
- 完全沿用文件现有风格，包括缩进方式、Tab/空格、分号和逗号尾缀。
- 仅保证新增或修改的行没有尾随空格，不批量清理其他行。
- 除空文件外，文件末尾必须有且仅有一个换行符 `\n`。
- 新建空文件时，优先遵循项目现有格式化配置（如 `.editorconfig`）；若无相关配置，则使用 UTF-8、LF 和 4 空格缩进。

## 交互与沟通

- 回答避免礼貌性废话，先说结论和核心方案，再提供必要依据。
- 向用户展示涉及布局、流程或架构的选项时，必须提供可视化预览，如 ASCII mockup、流程图、状态图或关键代码片段，使用户能直观对比差异。

## 任务完成审计与交付总结（强制）

### 适用范围与完成标准

- 本节适用于所有产生文件修改的任务；纯咨询和只读分析不适用。
- 接口包括与本次任务有关的公开 API、内部函数或方法、事件或信号、配置入口、数据读写以及第三方库调用，不得只检查对外 API。
- 审计、验证和交付总结全部完成前，不得宣告任务完成。

### 完成前审计

- 完成前再次沿实际调用链核对所有受影响的调用点、输入输出、数据流、状态权威写入者、生命周期、调用时序和失败路径，确认实际使用方式符合现有契约。
- 检查本次修改后不再使用的变量、字段、函数、方法、类型、导入、配置、分支、测试、文档和注释，并完全移除失效内容。
- 对仍然保留的旧路径、兼容层或看似可删除的定义，必须确认实际调用方或契约依据；不得仅以“可能有用”为由保留。
- 执行与改动风险匹配的测试、静态检查或人工核对，不得将未执行的验证描述为已通过。

### 最终交付检查表

最终答复必须逐项总结以下内容；某项没有相关内容时，也必须明确标记“不适用”并说明原因，不得直接省略。

1. **变更结果**：说明实际完成的行为、主要修改和影响范围。
2. **接口交互**：按实际调用链说明相关边界、调用方向、输入输出、状态或数据变化、调用时序、错误处理以及实际使用的现有接口。
3. **新增接口审计**：列出新增的 API、函数、方法、事件、配置入口或数据结构；没有新增时明确说明，有新增时说明现有能力为何不足以及新增的必要性。
4. **复用与系统一致性**：说明检索过的现有实现、最终复用的能力，以及实现如何符合现有职责边界和使用方式。
5. **清理结果**：列出已删除的失效定义、调用、分支及关联文档；保留旧路径或兼容代码时，说明实际调用方、契约依据和保留原因。
6. **设计原理与核心模型**：说明职责划分、状态权威写入者、生命周期所有者、关键数据流、选择当前方案的原因以及必要取舍；并明确回答“本次变动对系统核心模型有无破坏？”，说明核心契约变化或保持不变的依据。
7. **验证结果**：列出实际执行的测试或检查及其结果；存在未验证项时，说明原因和风险。

- 交付总结只放在最终答复中，不为单次任务额外创建永久报告；需要长期保留的契约和设计要求，仍按“文档与注释维护（强制）”执行。
