# 编码与协作规范

## 总体原则

- 优先满足最新需求，不为复用而复用。
- 设计方案应考虑完整，实现应保持简单；代码量应与功能复杂度匹配，并以满足需求的最少代码为目标。
- 优先使用 Godot 引擎已有模式或成熟第三方库，避免重复实现已有能力。
- 以实际代码逻辑为判断依据。文档和注释是理解实现的线索，但不得替代代码核对，也不得默认现有实现正确。
- 只给出确定、可执行的提示词和指导，禁止模糊描述。
- 当某项决策受到具体提示词影响时，明确指出对应提示词。

## 复用、函数与命名

- 多处相似逻辑应合并为通用函数，避免复制粘贴。
- 现有逻辑与新需求部分重合时，先评估能否直接改造；若改造会明显增加复杂度，则拆分职责并仅提取真正公共的部分。
- 不为形式统一强行拆分或封装。仅转发调用的包装函数应直接内联，例如 `on_enter`、`on_exit`、`on_fire` 只转调 `on_event` 时，不保留额外间接层。
- 函数名必须准确反映实际用途；含义和用途相同的函数、字段使用一致命名。

## 修改与重构

- 调整或重构函数前，检索并理解所有调用点、引用点和相关数据流，保证整条调用链同步修改且无遗漏。
- 方法、函数、字段或定义不再被使用时，必须完全移除，并同步删除对应的文档与注释。
- 工作流发生变化时，同步新增或更新提示词，明确指导 LLM 按新流程工作。
- 修复缺陷时，先稳定复现并定位根因，再实施修改，最后验证修复结果。
- 新增可配置内容时，必须在相应配置文件中提供配置入口。
- 方案调整后，当前计划只保留生效方案，不记录旧方案或调整过程。

## 简洁与优化

- 代码应简洁、清晰、健壮，不依赖隐晦约定。
- 收益明确且风险可控的优化，可结合实际场景主动实施，包括简化实现、减少资源消耗或改善性能。
- 收益不明确，且会显著增加实现复杂度、并发风险或架构约束的优化，必须先用性能数据验证；暂时无法验证时，先与用户沟通，不得擅自引入。
- 关键算法和设计模式优先采用中级程序员容易理解的简单实现。确需复杂算法或高级模式时，先说明必要性并获得用户批准。

## 计划要求

- 计划必须清晰列出涉及的具体文件、API、核心逻辑和关键算法。
- 计划只描述当前决定实施的方案，不保留已被替代的方案、失效方向或变更经过。

## 测试组织

- 功能测试应按功能集中管理，不得分散到被测配置或其他无关模块中。
- 涉及配置的功能，其测试仍归入该功能的测试位置，不在配置模块中单独编写。
- 功能较复杂时，在 `./tests` 下为该功能建立独立目录。

## 时间处理

- 时间在存储、传递和内部运算时统一使用时间戳。
- 仅在需要展示或格式化时，按运行时所在时区将时间戳转换为格式化时间。

## 文档与注释维护（强制）

### 阅读与判断

- 文档和注释可以概括代码逻辑、职责边界与关键流程；判断具体行为时，必须结合实际代码核对。
- 不得仅依据注释、文档、记忆或当前依赖组合推断系统行为。

### 准入标准

文档和注释只记录以下内容：

- 调用契约。
- 不可破坏的设计要求。
- 非显然的生命周期、线程或失败约束。
- 必须跨文件才能理解的修改规则。
- 帮助读者定位实现、选择配置或正确修改系统的必要信息。
- 文档引用实现位置时，不要求记录精确的 `path:line`；应定位到具体函数或字段，并提供可用于检索的名称或关键词。

不得记录以下内容：

- 对名称或代码的简单复述。
- 当前依赖组合或配置快照。
- 暂未实现的能力。
- 仅根据“当前代码中不存在”推导出的“不持有”“不负责”或“不依赖”等结论。
- 不影响契约、兼容性或安全性的普通当前值。

### 约束与术语

- 负面约束只有在违反它会构成缺陷，且能由仓库级规则、领域 Skill、测试或公开接口证明时才可保留。不得把对当前实现的观察提升为禁止性规则。
- 描述对象关系时，按实际含义分别使用：
  - “生命周期所有者”：负责对象的创建与释放。
  - “状态权威写入者”：拥有可变状态的修改权。
  - “依赖引用”：表示调用或引用关系。
- 不得使用“持有”笼统代替上述不同关系。

### 配置说明

- 需要帮助读者选择配置时，即使当前只有一个实际使用值，也应列举常用候选值，并说明各值的语义、影响、适用场景和约束。
- 除非具体值本身构成重要设计约束、兼容边界或安全要求，否则不得为同步现状而抄录 `foo = 1` 一类当前赋值。
- 必须记录具体值时，同时说明该值为何重要。

### 更新与删除

- 实现变化后，保留仍有效的契约并同步更新；已经过期、被取代或废弃的内容必须彻底删除，不得改写成“历史参考”“已废弃”等说明。
- 代码删除后，删除仅描述该代码的文档和注释。
- 当前活跃文档（包括本文件）不得保留失效计划、旧方向或旧设计；历史只通过 Git 追溯。
- 执行标准：宁可删除失效内容，也不要保留备注。
- 对“无法运行”“不可用”等结论，在写入最终记录或持久文档前，必须先与用户确认。

## 排版原则（最小化 Git 差异）

- 只修改与任务相关的逻辑和文本，不改动无关的缩进、引号或换行符类型。
- 完全沿用文件现有风格，包括缩进方式、Tab/空格、分号和逗号尾缀。
- 仅保证新增或修改的行没有尾随空格，不批量清理其他行。
- 除空文件外，文件末尾必须有且仅有一个换行符 `\n`。
- 新建空文件时，优先遵循项目现有格式化配置（如 `.editorconfig`）；若无相关配置，则使用 UTF-8、LF 和 4 空格缩进。
